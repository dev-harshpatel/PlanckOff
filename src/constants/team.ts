/**
 * Team-related types
 * Note: Team members are now fetched dynamically from the API
 * Use /api/team/dropdown for dropdown options
 */

export interface TeamMember {
  id: string;
  name: string;
}
